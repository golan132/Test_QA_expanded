testing:
  sampling:
    mode: "count"
    measurements_count: 100
    # total_duration_seconds is deliberately omitted in 'count' mode
    sampling_frequency_hz: 2.0
    timeout_seconds: 2.0
    acceptable_error_rate: 0.1

ammeters:
#  greenlee:
#    port: 5000
#    command: "MEASURE_GREENLEE -get_measurement"
#  entes:
#    port: 5001
#    command: "MEASURE_ENTES -get_data"
#  circutor:
#    port: 5002
#    command: "MEASURE_CIRCUTOR -get_measurement"

analysis:
  statistical_metrics:
  visualization:
    enabled: true
    plot_types:

result_management:
